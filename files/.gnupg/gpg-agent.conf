# Pinentry
pinentry-program /usr/bin/pinentry

# Cache passphrases for 2 hours, max 8 hours
default-cache-ttl 7200
max-cache-ttl 28800

# Allow loopback pinentry (needed for non-interactive / container use)
allow-loopback-pinentry
